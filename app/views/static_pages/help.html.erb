<% provide(:title, "New") %>
<!DOCTYPE html>
<html>
<head>
    <title><%= full_title(yield(:title)) %></title>
</head>
<body>
    <h1>New</h1>
    <%= render "navbar" %>

    <%= image_tag("flower.png", alt: "flower") %>
    </p> In the page you can add your own happiness.<p>
    </p> You can add what it is and what it means to you.<p>
    </p> Take sometime to reflect and think about where your happiness comes from.<p>
    <h2>Enter a name and add a new meaning for the name you choose.</h2>
    <h2>It can be anything as long as it makes you happy.</h2>

    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <%= form_for(@dataset) do |f| %>
            <%= f.label :name %>
            <%= f.text_field :name %>

            <%= f.label :add%>
            <%= f.text_field :add %>

            <%= f.submit "Create new happiness", class: "btn btn-primary" %>
            <% end %>
        </div>
    </div>


</body>
</html>

<%= render "foot" %>